# Crossplane v2 values for airgapped deployment
---
# Overrides for airgapped registry
image:
  repository: rune-registry.rune-registry.svc:5000/crossplane/crossplane

# Disable online features
webhooks:
  enabled: true

# Documentation examples for Providers and Functions should be added here
# when the specific cloud target is selected.
